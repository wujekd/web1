import { pubsub } from "../utilities/pubsub";

export const displayComponent = {

    displayGame: (game)=>{
        console.log(game);
    },


    displayLevel: (level) => {
        console.log(level)
    }
}